// Stubs for native/web compatibility across different tab screens to resolve circular references
export const openDrawer = () => {
  // Stub for native compatibility
};

export const setWebHeaderVisible = (visible: boolean) => {
  // Stub for native compatibility
};
